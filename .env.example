# Copy to .env and fill in. Never commit the real .env.
# AWS profile with ce:GetCostAndUsage permission.
# AWS_PROFILE=billing
# AWS_REGION=us-east-1
# Optional: Slack incoming webhook for --slack.
# SLACK_WEBHOOK=
